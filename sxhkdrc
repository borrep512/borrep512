# Terminal
ctrl + alt + Return
    kitty

# Cerrar ventana
super + q
    bspc node -close

# Mover foco
super + {h,j,k,l}
    bspc node -f {west,south,north,east}

# Lanzador de apps
super + d
    rofi -show drun

# Cambiar de escritorio
super + {1-9,0}
    bspc desktop -f {I,II,III,IV,V,VI,VII,VIII,IX,X}

